package com.ludoarena.model;

/**
 * Enum representing player colors on the Ludo board.
 * Each color corresponds to a specific starting quadrant and home path.
 */
public enum PlayerColor {
    RED,
    BLUE,
    GREEN,
    YELLOW
}
